---
"@plywise/chessboard": minor
"@plywise/chessboard-react": minor
---

Adds an `"inside"` coordinates mode rendering the full square name on every square. `coordinates` now accepts `boolean | "edge" | "inside"`; booleans keep their current meaning.