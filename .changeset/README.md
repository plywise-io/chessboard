# Changesets

Run `npm run changeset` for every user-visible package change. Select the affected packages, choose the semantic version bump, and describe the change in consumer-facing language.

Repository-only changes do not need a changeset.
